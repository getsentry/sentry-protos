syntax = "proto3";

package sentry_protos.billing.v1.services.quotas.v1;

message GetDashboardLimitRequest {
  uint64 organization_id = 1;
}

message GetDashboardLimitResponse {
  // Not set when the organization has no subscription.
  optional int64 limit = 1;
}
