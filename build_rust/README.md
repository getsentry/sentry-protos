# Build sentry_protos Rust bindings

This builds `sentry_protos` Rust bindings.

## Generating bindings

```shell
# In root of the repository
make build-rust

# Or with
cargo run -p build_sentry_protos
```
